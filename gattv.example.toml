# Hub config. Run with: uv run gattv hub
[telegram]
bot_token = "123456789:replace-me"
allowed_user_ids = [123456789]
notify_chat_ids = []

[hub]
listen_host = "0.0.0.0"
listen_port = 8765

[hub.cameras]
# Cameras are added when their setup is approved at the running hub.
